export type LoginData = {
    email: string;
    password: string;
  };
  
  export type RegisterData = {
    email: string;
    password: string;
    personName: string;
    position: string;
    companyName: string;
    phone: string;
    workEmail: string;    // NEW
    linkedIn: string;     // NEW
  };